# React + Vite


Sample react vite app with playwright  to get code coverage based on e2e test by playwright

1. npm i
2. npm run dev:coverage
3. npx playwright test or npx playwright test --ui
4. npm run coverage:report
5. open coverage/index.html (.nyc_output/ = raw coverage)
